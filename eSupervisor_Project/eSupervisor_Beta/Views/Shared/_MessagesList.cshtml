@model IEnumerable<eSupervisor_Beta.Hubs.messageContainer>

@foreach (var item in Model)
{
    if (item.senderID == (int)Session["userid"])
    {
        <li class="left clearfix">
            <span class="chat-img pull-left">
            </span>
            <div class="chat-body clearfix">
                <div class="header">
                    <strong class="primary-font">@item.senderName</strong>
                    <small class="pull-right text-muted">
                        <i class="fa fa-clock-o fa-fw"></i> just said
                    </small>
                </div>
                <p>
                    @item.content
                </p>
            </div>
        </li>
    }
    else
    {
        <li class="right clearfix">
            <span class="chat-img pull-right">
            </span>
            <div class="chat-body clearfix">
                <div class="header">
                    <small class=" text-muted">
                        <i class="fa fa-clock-o fa-fw"></i> just said
                    </small>
                    <strong class="pull-right primary-font">@item.senderName</strong>
                </div>
                <p>
                    @item.content
                </p>
            </div>
        </li>
    }
}



    
    
